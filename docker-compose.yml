version: '2'

services:
  api:
    image: vibioh/dashboard-api
    command:
    - -dockerVersion
    - '1.24'
    - -ws
    - ^dashboard(?:-api)?\.vibioh\.fr
    - -authUrl
    - https://auth.vibioh.fr
    - -users
    - vibioh:admin|lili1725:multi
    - -corsHeaders
    - Content-Type, Authorization
    - -corsMethods
    - GET, POST, DELETE
    labels:
      traefik.frontend.passHostHeader: 'true'
      traefik.frontend.rule: 'Host: dashboard-api.vibioh.fr'
      traefik.port: '1080'
    volumes:
    - /var/run/docker.sock:/var/run/docker.sock:ro
    logging:
      driver: json-file
      options:
        max-size: '50m'
    restart: on-failure:5
    read_only: true
    cpu_shares: 128
    mem_limit: 33554432
    security_opt:
    - no-new-privileges

  front:
    image: vibioh/dashboard-front
    command:
    - -spa
    - -env
    - API_URL,WS_URL,AUTH_URL,BASIC_AUTH_ENABLED,GITHUB_OAUTH_CLIENT_ID,GITHUB_OAUTH_STATE
    - -csp
    - "default-src 'self'; script-src 'self' 'unsafe-inline' www.google-analytics.com cdnjs.cloudflare.com ajax.googleapis.com cdn.ravenjs.com; style-src 'self' 'unsafe-inline' fonts.googleapis.com cdnjs.cloudflare.com; font-src 'self' fonts.gstatic.com cdnjs.cloudflare.com data: ; img-src 'self' www.google-analytics.com data: ; connect-src 'self' wss: dashboard-api.vibioh.fr auth.vibioh.fr;"
    labels:
      traefik.frontend.passHostHeader: 'true'
      traefik.frontend.rule: 'Host: dashboard.vibioh.fr'
      traefik.port: '1080'
    environment:
      API_URL: 'https://dashboard-api.vibioh.fr'
      WS_URL: 'wss://dashboard-api.vibioh.fr/ws'
      AUTH_URL: 'https://auth.vibioh.fr'
      BASIC_AUTH_ENABLED: 'false'
      GITHUB_OAUTH_STATE: '${GITHUB_OAUTH_STATE}'
      GITHUB_OAUTH_CLIENT_ID: '${GITHUB_OAUTH_CLIENT_ID}'
    logging:
      driver: json-file
      options:
        max-size: '50m'
    restart: on-failure:5
    read_only: true
    cpu_shares: 128
    mem_limit: 67108864
    security_opt:
    - no-new-privileges

networks:
  default:
    external:
      name: traefik
